name: deno fmt & lint

on:
  pull_request:

jobs:
  fmt_lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
      - uses: denoland/setup-deno@v2

      - name: deno fmt
        run: deno fmt --check

      - name: deno lint
        run: deno lint
